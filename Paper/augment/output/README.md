# Augmentation review files

Generated by `Paper/augment/corrupt.py` (seed 42, 11623 instances).

- `review_negatives.tsv` -- precision probes (gold=0).
- `review_positives.tsv` -- recall probes, incl. oracle-blind (gold=1).
- `candidates_review.tsv` -- candidate word list per rhyme class.

## Column usage note

```
pron columns = Thai-script pronunciation for cross-checking only.
  source 'override' = curated POETRY_OVERRIDES (authoritative);
  source 'w2p' = pythainlp w2p ML model -- CAN hallucinate, especially on short / multi-syllable words; treat as a HINT, verify with IPA and syllable decomposition, never trust it as ground truth.
partner = the rule's rhyme-target syllables (the avoid set), each with its สระ and มาตรา as read by the 5.3.5 oracle -- side C must rhyme with positive items and must not rhyme with negative items.
```
